import styled from "styled-components";

export const RelativeContainer = styled.div`
    position: relative;
    cursor: pointer;
  `;

export const SelectablePopupContent = styled.div`
  cursor: initial;
`;
