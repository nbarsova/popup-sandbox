import React from 'react';
import {HeaderPopupContent} from "./ui";

const HeaderPopup = () => {
    return (
        <HeaderPopupContent>
            App header has a simple hoverable popup positioned strictly (always center left)
        </HeaderPopupContent>
    );
};

export default HeaderPopup;
