import React from 'react';
import {FooterPopupContent} from "./ui";

const FooterPopup = () => {
    return ( <FooterPopupContent>
                Positioned on top automatically
        </FooterPopupContent>
    );
};

export default FooterPopup;
